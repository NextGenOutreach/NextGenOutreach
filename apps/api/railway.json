{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=packages/types && npm run build --workspace=packages/validators && cd apps/api && npx prisma generate && npm run build && npx prisma db push --accept-data-loss"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/server.js",
    "healthcheckPath": "/live",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
